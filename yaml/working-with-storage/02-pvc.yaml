apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devpvc
  namespace: cloudnativeapp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: datastore
  resources:
    requests:
      storage: 5Gi
